-- lazy.nvim configuration
return {
  'tom-doerr/vim_codex',
  dependencies = { 'nvim-lua/plenary.nvim' }, -- Example dependency, check specific plugin requirements
  config = function()
    -- Optional: Configure plugin settings here
  end,
}
